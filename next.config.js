/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Add any experimental features you want to enable, if any
  },
  // Add other Next.js configuration options here
};

module.exports = nextConfig;